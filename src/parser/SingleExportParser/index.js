import AbstractParser from './AbstractParser';

class SingleExportParser extends AbstractParser {
	constructor(file) {
		super(file);
	}

	parse()
}

const SingleExportParser = {


	parse: (file) => {
	},
	write: () => {
	}
};